meta {
  name: delete_category_success
  type: http
  seq: 5
}

delete {
  url: {{apiUrl}}/api/v1/categories/{{testCategoryId}}
  auth: none
}

headers {
  Authorization: Bearer {{idToken}}
  Content-Type: application/json
}

tests {
  test("Status is 204 (No Content)", function() {
    expect(res.status).to.equal(204);
  });
  test("Response body is empty", function() {
    expect(res.body).to.be.empty;
  });
  // Save a variable to verify deletion
  bru.setEnvVar("deletedCategoryId", bru.getEnvVar("testCategoryId"));
}
