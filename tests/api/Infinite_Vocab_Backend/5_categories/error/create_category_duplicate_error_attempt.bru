meta {
  name: create_category_duplicate_error_attempt
  type: http
  seq: 3
}

post {

  url: {{apiUrl}}/api/v1/categories/
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{idToken}}

  Content-Type: application/json
}

body:json {
  {
    "categoryName": "test duplicate",
    "categoryColor": "#654321"
  }
}

tests {
  test("Status is 400", function() {
    expect(res.status).to.equal(400);
  });
  
  test("Response has duplicate error message", function() {
    expect(res.body).to.have.property('error');
    expect(res.body.error).to.include('already exists');

  });
}
