meta {
  name: unlink_word_from_category
  type: http
  seq: 5
}

delete {
  url: {{apiUrl}}/api/v1/categories/{{linkTestCategoryId}}/words/{{linkTestWordId}}
  auth: none
}

headers {
  Authorization: Bearer {{idToken}}
}

tests {
  test("Status is 200 OK", function () {
    expect(res.status).to.equal(200);
  });
  
  test("Response has success message", function () {
    expect(res.body).to.have.property('message');
    expect(res.body.message).to.include('successfully removed');
  });
}
