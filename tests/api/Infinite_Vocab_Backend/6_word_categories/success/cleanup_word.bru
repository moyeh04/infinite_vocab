meta {
  name: cleanup_word
  type: http
  seq: 6
}

delete {
  url: {{apiUrl}}/api/v1/words/{{linkTestWordId}}
  auth: none
}

headers {
  Authorization: Bearer {{idToken}}
}

tests {
  test("Cleanup Word Successful", function() {
    expect(res.status).to.equal(200);
  });
}
