meta {
  name: cleanup_category
  type: http
  seq: 7
}

delete {
  url: {{apiUrl}}/api/v1/categories/{{linkTestCategoryId}}
  auth: none
}

headers {
  Authorization: Bearer {{idToken}}
}

tests {
  test("Cleanup Category Successful", function() {
    expect(res.status).to.equal(200);
  });
}
