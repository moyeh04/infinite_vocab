meta {
  name: create_category_for_linking
  type: http
  seq: 2
}

post {
  url: {{apiUrl}}/api/v1/categories/
  body: json
  auth: none
}

headers {
  Authorization: Bearer {{idToken}}
  Content-Type: application/json
}

body:json {
  {
    "categoryName": "Science Branches",
    "categoryColor": "#2E86C1"
  }
}

vars:post-response {
  linkTestCategoryId: res.body.categoryId
}

tests {
  test("Status is 201", function() {
    expect(res.status).to.equal(201);
  });
}
