meta {
  name: unlink_non_existent_link_error
  type: http
  seq: 2
}

delete {
  url: {{apiUrl}}/api/v1/categories/{{linkTestCategoryId}}/words/{{linkTestWordId}}
  auth: none
}

headers {
  Authorization: Bearer {{idToken}}
}

tests {
  test("Status is 404 Not Found", function () {
    expect(res.status).to.equal(404);
  });
  
  test("Error message for non-existent link", function () {
    expect(res.body.error).to.include('not in the specified category');
  });
}
