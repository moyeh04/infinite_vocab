meta {
  name: create_word_missing_body
  type: http
  seq: 1
}

post {
  url: {{apiUrl}}/api/v1/words/
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{idToken}}
  Content-Type: application/json
}

tests {
  test("Status is 400", function() {
    expect(res.status).to.equal(400);
  });
}
