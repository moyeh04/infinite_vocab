meta {
  name: update_description_missing_body
  type: http
  seq: 14
}

patch {
  url: {{apiUrl}}/api/v1/words/dummy_word_id/descriptions/dummy_description_id
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{idToken}}
  Content-Type: application/json
}

tests {
  test("Status is 400", function() {
    expect(res.status).to.equal(400);
  });

  test("Response has error field", function() {
    expect(res.body).to.have.property('error');
  });

  test("Error message mentions missing or invalid JSON", function() {
    expect(res.body.error.toLowerCase()).to.match(/(missing|invalid|json|body)/);
  });
}
