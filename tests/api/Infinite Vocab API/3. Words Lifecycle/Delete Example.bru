meta {
  name: Delete Example
  type: http
  seq: 12
}

delete {
  url: {{appUrl}}/api/v1/words/{{wordId}}/examples/{{exampleId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}
