meta {
  name: Delete Description
  type: http
  seq: 11
}

delete {
  url: {{appUrl}}/api/v1/words/{{wordId}}/descriptions/{{descriptionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{userToken}}
}
